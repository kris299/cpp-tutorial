#include <iostream>
using namespace std;

// functions
// need to be declared before main function.

int myfunction(int num1, int num2);

// similar to declaration of variables, the declaration starts with a type, which
// the funtion returns, followed by space and the actuall function name and
// then followed by optional arguments in parenthesis. 
// The type of the arguments has to be declared aswell.
// The example function takes to integer types as input and returns an integer type.
//

// the actuall body of the function then may be defined outside the main function 
// and in different files.


int main()
{
	int a = 5;
	int b = 9;
	cout << "a = " << a << endl;
	cout << "b = " << b << endl << endl;
	cout << "calling myfunction(a, b) results in: " << myfunction(a,b) << endl;

	return 0;
}


// Actual definition of the body of the function.

int myfunction(int num1, int num2)
{
	int result;
	result = num1 * num2;
	return result;
}

